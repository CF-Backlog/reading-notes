# Context API

Describe use cases for useMemo() and useReducer()
> useMemo saves a value and only runs it if it changes.
>useReducer is similar to useState but more suited to complex components with sub-values.

Why do custom hooks need the use prefix?
> use denotes a function that will maintain all hook rules.

What do custom hooks usually do?
> helps with versatility and reusability with respect to the thing it was meant to do - karma

Using any list of custom hooks, research and name one that you think will be useful in your applications
> useLocalStorage seems nice, because saving data locally so i dont have to keeping signing in during a session or at all is a nice feature we want.

Describe how a hook that fetches API data might work
> useFetch is the counterpart of fetch, grabs up the info and runs away with it, or sets up state for isLoading

reducer - f(x) uses received action to determine changes to app state

Which 3 things had you heard about previously and now have better clarity on?

Which 3 things are you hoping to learn more about in the upcoming lecture/demo?
> useReducer

What are you most excited about trying to implement or see how it works?
